<% if @books.present? %>
  <% @books.each do |book| %>
    <%= link_to (image_tag(book.image, style: "width: 70px;height: 100px;display: inline-block;")), book.detail %>
    <%= book.title %>
    <br>
  <% end %>
<% end %>

